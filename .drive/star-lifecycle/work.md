# work — star-lifecycle

DRIVE_STATUS: done (2026-08-17 · round-44)

**세 묶음이 전부 채워졌다.** ①사용자가 요청한 셋(넷을 눈으로 가른다) ②`spec.md`
「이번 범위」 미충족 **0개** ③`report-deferred.md` 「미룬 것」 전부 처리(구현 셋 ·
근거 있는 버림 셋 · 사유 갱신 둘). 보고 3종은 `report-done.md` · `report-deferred.md` ·
`report-review-me.md`.

## 진행 상황 (2026-08-17 · round-33 까지)

**사용자가 오늘 요청한 셋 — 전부 끝남**
- round-26 온도를 밝기와 분리 · **점 렌더가 실제 화면 경로임을 찾음**(격자만 고치면 화면 안 바뀜)
- round-27 중성자별 잔해 + 펄서 깜빡임(변동계수 34.2%)
- round-28 블랙홀 강착원반 + **에딩턴 강착 한계**(없으면 블랙홀이 판을 삼킨다. 833k → 67k)

**spec 「이번 범위」에서 채운 것**
- round-29 초신성이 터져도 배경 유지(101.5%)
- round-31 같은 별이 늘 같게 쪼개짐(0.001%) · 줌 오가도 안 튐(1.05배)
- round-32 줌아웃 한 점 ↔ 줌인 알갱이(스크린샷 3장) · 성단 생김
- round-33 폭발 자리에서 새 별(재 850배)

- round-34 **암흑물질**(회전곡선 바깥/안쪽 0.607 → 0.873)

### **spec.md 「이번 범위」 미충족 = 0 개** (2026-08-17 확인)

## 셋째 묶음 진행 — `report-deferred.md` 「미룬 것」

- round-35 **반사성운** — 스케일은 고쳤으나 화면에 안 나온다. **벽은 렌더 경로였다**
  (성운은 격자 경로에만 있는데 실사용 배율에서는 점 렌더만 돈다 — round-26 과 같은 벽).
  「설정이 코어에 닿았는지」 보는 창(`coreNebulaK` 등)을 새로 냈다
- round-36 **별풍** — 넣었고 별의 최후를 바꾸지만 **가스 고갈은 못 풀었다.**
  그 대신 **원인이 좁혀졌다**: 가스가 안 돌아오는 것이 아니라 **돌아온 가스가 즉시 다시
  별이 된다.** 별 형성 속도가 빨라 평형이 「전부 별」에 선다

- round-37 **가스 고갈이 풀렸다** — 답은 `starFormEfficiency` 한 값(0.02 → **0.005**).
  0.02 에서는 스텝당 통과 확률이 0.0038 인데 40초면 1200 스텝이라 99% 가 통과해 효율이
  **사실상 없었다.** 실측: 가스 0.13% → **7.48%**(58배), 화면 밝기 14.88 → **25.18%**(+69%),
  폭발 1 → **14개**. 셋이 동시에 좋아진다. 실제 거대 분자운의 효율 1~5% 안이다

- round-38 **앱을 열면 밀도로 시작해 이 판이 만든 것의 절반이 안 보였다** — round-25 와
  같은 함정. 지금까지 모든 측정이 `colorBy=light` 를 명시로 보내고 쟀다. 아무것도 안 보내고
  띄우니 밀도 모드였다. 고치는 데 두 걸음: `look` 기본값 변경 + **`ApplyLook` 이 「설정
  초기화」 버튼에서만 불려** 시작 때 짝이 안 맞던 것(`App::init` 에서 부르게 함).
  실측: 붉은 0.7% → **47.2%**, 청록 90.4% → 0.10%. 같은 판에서 가스 7.09% · 폭발 14 ·
  중성자별 145 · 블랙홀 8 (round-37 의 효율 변경이 여기서 확인됨)

### 다음에 할 것 (우선순위 순)

- round-39 **강착원반 바깥 끝을 12배 → 30배**(실제 원반은 수백~수천 r_s 까지 뻗는다).
  밝기 지수를 −2 로 완만하게 해 봤다가 **되돌렸다** — 청록은 0.10 → 0.36% 로 늘었지만
  켜진 픽셀이 32 → 4% 로 무너졌다. **바깥이 안 보이는 것은 버그가 아니라 물리다**
  (30배 자리에서 2만 7천분의 1). 줌인하면 보인다.
  **알게 된 것: 켜진 픽셀 비율은 같은 설정에서도 13.55~32.23% 로 흔들린다** — 판이
  카오스적이라 같은 시뮬 시간의 구조가 매번 다르다. **이 값 하나로 회귀를 판정하면 안 된다**

- round-40 **반사성운이 드디어 화면에 나온다** — 여섯 바퀴 걸렸다(20·21·35·40).
  물음이 두 번 옮겨갔다: 섞는 비율 → 스케일 → **렌더 경로가 아예 다르다**.
  가스 알갱이가 자기 자리의 퍼진 별빛을 **읽는다**(이웃을 안 훑는다 — 원자 연산 없는
  읽기 두 번). 실측: 켜진 픽셀 32.2% → **50.4%**, 청록 0.11% → **2.09%**, stepMs 11.5

- round-41 **미룬 것 셋을 「버린 것」으로 내렸다** — 셋 다 근거를 붙였다.
  ①**분산 텐서 교차항**: 창을 만들어 실측(추정이었던 것을 확인으로 바꿨다). 절댓값 비는
  0.43~0.64 로 커 보이지만 **상관 없는 등방 난류의 기댓값(0.637)과 같아** 아무것도 안
  가른다. **부호를 살리면 0.022 / 0.00001 / −0.0255 로 20~40배 작다** — 부호가 무작위라
  격자에 쌓으면 상쇄된다. ②**성운 RGB 3채널**: 전제 둘이 다 무너졌다(성운은 FFT 를 안
  쓰고, 색은 온도 격자로 이미 처리한다). ③**핵융합 자기조절**: 사용자가 범위에서 뺀 것이라
  이 판이 되돌릴 결정이 아니다

- round-42 **암흑성운** — 반사성운이 쓰는 가스 기둥 격자가 이미 시선 투과도였다(같은 가스가
  앞쪽 빛은 반사하고 뒤쪽 빛은 가린다). 실측: 켜진 픽셀이 dustK 0→0.3→1.5→5.0 에서
  **28.44 → 27.45 → 23.62 → 16.38%**. 스크린샷에 **필라멘트를 따라 검은 갈라짐**이 보인다

### 남은 셋 — 각각의 다음 행동

**① 재 색 은하 나이 지도** — 색 채널이 온도에 이미 쓰이고 있어 **별도 보기 모드**
(`ColorBy::Ash`)가 맞다. `ashGrid` 를 투영해 그리면 되므로 커널 하나. **다음에 이것**

**③ 별 질량을 중력에 반영** — **크고 위험하다.** `kScatter` 가 무게 1 로 뿌리고 퍼텐셜도
총 개수로 정규화한다. 질량을 넣으면 밀도 격자의 스케일이 통째로 바뀌어 **중력 세기가
달라지고 판이 다른 것이 된다.** 넣으려면 정규화를 총 질량 기준으로 다시 잡고 이 판의
모든 실측을 다시 떠야 한다

**④ 별이 모여 은하가 되는 과정** — 초기 조건과 판 크기를 다시 잡는 **별개 주제**다.
이 판의 주제는 「이미 있는 은하 안에서 별의 한살이」이고, 그 앞 단계는 다른 판이다

**3. 나머지**: 분산 텐서 교차항 · 별 질량을 중력에 반영 · 재 색 은하 나이 지도 ·
성운 RGB 3채널 · 암흑성운 · 별이 모여 은하가 되는 과정 · 핵융합 자기조절

**미충족으로 남은 것(사유 있음)**: 가스 1% 유지(round-30 — 근본 원인은 시간 눈금이고
물리값의 200배를 쓰면 닿지만 원칙 1 에 어긋나 안 씀)

**커밋**: cab1054 · ea895d4 · 71bdc42 · 2b6a7c3 · e3ded35 · 95a66c9 · 9673760
resume_cron: 96458b96 (17,47)
resume_idle_count: 0   (round-24 에서 파일 넷이 늘거나 바뀌었다 — spec.md · work.md · round-24-spec-compare.md · report-deferred.md)

## VCS

- 모드: Git
- 작업 브랜치: **`feat/star-lifecycle`** (main 에서 분기, 2026-08-16)
- 베이스라인: `d5f886b` — feat: 강한핵력·전자기력·약한핵력 — 폭주 원인 일곱 개를 찾아 고쳤다
- 분기 시점 작업 트리: 깨끗(`git status --porcelain` 이 `?? .drive/star-lifecycle/` 하나)
- **모든 바퀴가 이 브랜치에 쌓인다.** 커밋·푸시·PR 은 사용자 승인 없이 하지 않는다

## permissions_preflight

0번 칸(권한 예열)을 별도로 돌지 않았다 — 이 판은 앞 세션에서 brew 로 시작해 체인 중간에
들어왔고, 그 세션 내내 빌드·실행·제어 채널·git 읽기가 이미 승인 없이 돌고 있었다.
새로 필요한 권한이 나오면 그 자리에서 받는다.

## 이 세션의 구조적 제약 (판정에 영향 있음)

**서브에이전트 호출 금지** — 세션 지침에 `Do not call the AgentTool unless the user requested it`
이 걸려 있다. `CAP:drive` 는 4·6·7번 칸을 서브로 보내라고 하지만, 같은 문서의
"서브에이전트를 쓸 수 없는 세션이면 그 칸을 메인에서 직접 돌고 「메인 실행」으로 적는다.
칸을 건너뛰지 않는다" 에 따라 **메인에서 돈다.**

잃는 것을 명시한다 — 4번 칸이 서브로 도는 이유는 **스펙을 쓴 컨텍스트가 자기 누락을 찾으면
흘린 이유가 그대로 눈가리개로 남기 때문**이다. 이 판은 그 보호를 못 받는다.
1차 판정에서 누락 5건을 잡았지만 같은 눈이 못 본 것이 더 있을 수 있고,
그 사실을 `report-review-me.md` 에 올린다.

## 판단 기록

- **[round-26 · 발견] 화면에 나오는 것은 격자가 아니라 점 렌더다** — `pointMix = (cellPx-1)/3` 이고 배율 1.78·격자 128 이면 한 칸이 12.5 픽셀이라 1 로 포화된다. 그러면 `wantField` 가 거짓이 되어 **격자는 아예 안 그려진다.** 온도 축을 격자에만 넣고 재니 어두운 픽셀이 100% 파랗게 나왔고, 픽셀 값을 직접 재니 `r/b = 0.287` 로 **`cmapBlackbody` 가 낼 수 없는 색**(가장 푸른 끝에서도 0.62)이었다 — `cmapAstro` 의 정지점 (0.05,0.06,0.19) 이 0.263 이었다. 근거: `RenderField.cu:486` `wantField`, `scratchpad/r26c-pix.ps1` 출력
- **[round-26 · 앞 바퀴를 흔든다] round-16 의 색 판정이 흑체가 아니었을 수 있다** — 「붉은 2.6% · 흰 76.7% · 푸른 20.7%」를 같은 배율에서 쟀다면 천체 컬러맵을 세었다. **다만 round-26 이 같은 항목을 실제 흑체로 다시 재 31.6/56.2/12.2 를 얻었으므로 판정은 유지하고 근거만 갈아 끼운다**
- [round-26 · 5번 칸] 채도를 3배로 **들었다** — 근거: 실제 흑체의 청백은 40000K 에서도 (0.59,0.73,1.00) 이라 빨강과 4% 차이고, 그 상태에서 「푸른 별」로 세어진 픽셀이 **0.0%** 였다. 회색축에서 밀어내는 것은 색조를 안 바꾸고 대비만 키우는 처리라 **없는 구조를 그리는 연출과 다르다**(실제 천체 사진이 하는 것과 같다). 원칙 1 에 걸리는지 갈리는 자리라 `report-review-me.md` 에 올린다
- [round-26 · 5번 칸] 명도에 `√t` 를 씌웠다 — 근거: 밝기로 색을 정하던 시절 `cmapBlackbody(0)` 의 휘도가 **0.50** 이라 컬러맵이 명도 바닥을 깔고 있었다. 색을 온도로 옮기자 그 바닥이 사라져 켜진 픽셀이 6.4% → 1.34% 로 떨어졌다. 감마를 건드리면 다른 색 기준까지 바뀌므로 이 경로에서만 쓴다
- [round-27 · 5번 칸] 중성자별 심을 **확률로** 남긴다(전부 아님) — 근거: 알갱이 하나를 둘로 쪼갤 수 없는데 실제 초신성은 심 하나 + 가스 여럿을 남긴다. 확률을 「심 질량 ÷ 원래 질량」으로 두면 무리 전체의 심 총질량이 실제 비율과 같아진다. **다 남기면 「사슬이 닫힌다」의 마지막 고리가 끊긴다** — 이미 `cGas` 가 3 까지 내려가 있다
- [round-27 · 5번 칸] 펄서 밝기를 **열복사 밖에서** 준다(`1000·beam`) — 근거: 반지름 10km 라 `L=4πR²σT⁴` 로는 태양의 1조분의 1 이라 한 점도 안 찍힌다. 실제 펄서가 보이는 이유는 열복사가 아니라 회전 에너지를 뽑는 싱크로트론 복사여서다. **흑체 공식 밖이라는 것이 근거이지 임의 배율이 아니다**
- [4번 칸 · spec-trace] 서브에이전트 금지 세션 → **메인에서 직접 실행.** 칸을 건너뛰지 않는 쪽을 택했다 — 근거: `CAP:drive` SKILL.md 「어디서 도는가」의 "서브는 컨텍스트 절약 수단이지 칸의 존재 조건이 아니다"
- [4번 칸 · spec-trace] 1차 FAIL(누락 5건) → **drive 가 `spec.md` 에 되돌림.** 사용자에게 새로 묻지 않았다 — 근거: `CAP:drive` SKILL.md 「되돌리기는 범위 확대가 아니다」. 앞 칸에서 이미 합의한 항목의 전사 오류 수정이다. 상세는 `spec-trace.md` 2차 판정
- [4번 칸 · spec-trace] N5(재의 양을 색으로 = 은하 나이 지도)를 **미룬 것으로 내림** — 근거: 빛 렌더를 밝기 1채널로 줄인 설계(`design.md` 「빛으로 본다」)라 재 색을 얹을 채널이 없다. 사용자 "아주 좋은 아이디어야"(`log.md:174`)의 대상인 재 사슬 **본체는 이번 범위에 그대로 있다**
- [4번 칸 → 5번 칸] 브랜치 `feat/star-lifecycle` 생성. main 이 기본 브랜치라 거기서 직접 작업하지 않는다
- [3번 칸 · brainstorming] 압력을 **뺐다가 텐서로 되돌림** — 근거: 코덱스 리뷰가 등방 압력의 물리 오류를 지적해 1차로 뺐으나, 사용자 질문("압력없어도돼?")으로 재검토한 결과 압력이 없으면 `orbitDispersion`·`diskThickness`·별 문턱이 전부 손으로 정한 숫자로 남아 원칙 1(연출 제거)이 크게 후퇴한다. 평균 흐름 `v̄` 를 빼고 방향별 σ²ᵢᵢ 셋을 드는 형태로 오염원을 피한다

## 이번 범위 진행

**41개 중 4개 완료** (round-01).

| 바퀴 | 증분 | 커밋 | 충족 | 누적 |
|---|---|---|---|---|
| round-01 | 안전 바닥과 시간 배율 | `42497e1` | 4 | 4/44 |
| round-02 | 압력 텐서 | `19bf0d6` | 3 | 7/44 |
| round-03 | 수치 가열 잡기(8→2 주기) | `5340c17` | 1 | 8/44 |
| round-04 | 별 문턱(Jeans) + `starCount` | `a7fb94e` | 2 | 10/44 |
| round-05 | 수명·폭발 — 사슬 닫기 | `99bc170` | 0 | 10/44 |
| round-06 | 별 비율 눈금(sunMass 100→3) | `b1b6859` | 1 | 11/44 |
| round-07 | 재 사슬 — 세대가 작아진다 | `1fb6245` | 2 | 13/44 |
| round-08 | 블랙홀 전환 | `859c217` | 2 | 15/44 |
| round-09 | 연출 걷어내기(난수·밀도파·헤일로) | `13af736` | 1 | 16/44 |
| round-10 | 나선팔 배치 제거 + 되먹임 제동 | `daec333` | 1 | 17/44 |
| round-11 | 빛으로 보기(구현만, 화면 미연결) | `ed96ce6` | 0 | 17/44 |
| round-12 | 빛 모드 화면 연결 | `c91a8af` | 1 | 18/44 |
| round-13 | 보존량 측정 | `b874804` | 2 | 20/44 |
| round-14 | 구간별 시간 | `b0cbeab` | 1 | 21/44 |
| round-15 | 이미 선 근거를 스펙에 반영 | (코드 변경 없음) | 6 | 25/44 |
| round-16 | 흑체 색 — 색이 늙는다 | `d78ae62` | 2 | 27/44 |
| round-17 | 창발 관찰(나선팔·금속 기울기) | `2cfe486` | 2 | 29/44 |
| round-18 | 재 역전 원인 가르기(진단) | `dd71ddd` | 0 | 29/44 |
| round-19 | 은하 이동 추적 · **정체 발동** | `bb99294` | 0 | 29/44 |
| round-20 | 성운 반사광(구현만, 정규화가 상쇄) | `8c2b78b` | 0 | 29/44 |
| round-21 | 밝기 백분위수 · **성운을 미룬 것으로** | `a6c2f6f` | 0 | 29/43 |
| round-22 | 원반 두께를 재는 창 · **차단 항목 발견** | `5397bc1` | 3 | 34/44 |
| round-23 | 화면이 은하를 따라간다 · round-22 정정 | `22b3173` | 1 | 35/44 |
| round-24 | **표류의 원인 = 식히기** · 세 바퀴짜리 물음 해결 | `9a08cbb` | 1 | 36/44 |
| round-25 | **앱을 열면 별이 0 개였다** · 나선팔 판정 뒤집음 | `5398db8` | 1 | **37/44** |

- **[round-25 · 발견 · 가장 큼] 장면 기본값이 별 형성을 안 켜고 있었다** — 코어 기본이 `false`(`Sim.h`)인데 `App.cpp` 가 안 켰다. **앱을 열어 그냥 두면 이 판이 만든 것이 하나도 안 나온다.** 스물다섯 바퀴 동안 못 본 이유는 **측정 스크립트가 매번 `starFormation=1` 을 명시로 켜고 쟀기 때문**이다. **재발 방지: 사용자가 보는 것을 확인할 때는 아무것도 켜지 말고 기본값 그대로 띄운다 — 스크립트가 켜 주는 것은 사용자가 보는 것이 아니다**
- **[round-25 · 정정] round-17 의 「나선팔 0.5~0.8」은 팔이 아니라 실이었다** — 화면으로 보니 판이 한 줄기 호였다. `spiralM2` 는 각도가 두 방향에 몰리면 커지는데 **긴 호도 양끝이 두 방향이라 크게 나온다.** **수치 하나로 형태를 판정한 것이 틀렸다** — round-17 이 남긴 「스크린샷 대조가 남았다」를 이번에 했고 판정이 뒤집혔다
- [round-25 · 5번 칸] 실을 만드는 것을 **냉각 하나로 좁혔다** — 냉각 끄면 `spiralM2` 0.789 → 0.006. round-18 이 「중력·냉각·압력」으로 좁힌 것을 더 좁혔다. 냉각이 이웃 평균으로 당기며 **차등회전까지 지운다**
- [round-25 · 5번 칸] `coolingRate` 0.25 → 0.08 — 근거: 스캔에서 45초 `spiralM2` 가 0.106 으로 실제 은하 범위에 들고 최대밀도(뭉침)는 유지된다. **그러나 100초까지 돌리면 0.719 로 되돌아간다 — 늦추는 값이지 막는 값이 아니다.** 그리고 **이 값은 실측 튜닝이지 물리가 정해 준 것이 아니다**(주석에 그렇게 적었다)
- **[round-25 · 원칙 2 발동과 해소] 「빠진 현실」은 초신성 되먹임이었고 이미 만들어 둔 것이었다** — 넣을 것이 아니라 **켤 것**이었다. 켜니 `spiralM2` 가 100초까지 0.10~0.26 으로 머물고 화면에 두 팔이 대칭으로 보인다. **한 바퀴로 끝나 「최대 세 바퀴」를 다 안 썼다**
- [round-25 · 5번 칸] round-24 가 「줄인 것」으로 넘긴 **`ashCells` 판별식을 찍었다** — 칸당으로 재니 초기 안/바깥이 **479~2365배**로 실제 은하와 **같은 방향**이다. 총량으로 바깥이 많아 보이던 것은 바깥 구간의 칸 수가 많아서였다

- **[round-24 · 발견 · 가장 큼] 은하가 판 밖으로 흘러가던 원인은 식히기였다.** 중력만 켠 판은 무게중심이 전혀 안 움직인다(100만·10만 둘 다). 하나씩 켜 보니 **냉각만** 움직였다 — 40초에 0.303 이사·운동량 128,426. 압력만 0.0003·211, 별만 0.0003·0.2
- [round-24 · 5번 칸] **공유 전제부터 쟀다**(round-18 재발 방지 적용) — 후보를 나열하기 전에 「이동이 등속인가 가속인가」를 먼저 봤고, **가속**이라 「초기 킥 하나」 계열 후보가 통째로 떨어졌다. 그다음 하나씩 켜서 갈랐다
- [round-24 · 5번 칸] **증상을 지우고 원인은 주석에 남겼다** — `kCool` 의 `k/n` 비대칭은 「이웃 평균으로 끌린다」는 형태 자체에서 오고, 쌍 대칭으로 고치면 원자 연산이 이웃 수만큼 늘어 이 커널이 가장 비싼 자리가 된다. **지우는 것은 알짜 흐름뿐이고 국소적 힘은 남는다는 것을 코드에 못 박았다**
- [round-24 · 5번 칸] 평균을 **GPU 안에서** 낸다 — 근거: 호스트로 가져오면 스텝마다 `cudaMemcpy` 동기화가 GPU 를 멈춘다. `kMomentumAccum` 넷째 칸에 개수를 세고 나눗셈은 커널이 한다. **그 커널을 부르는 자리 둘의 memset 을 3 → 4 칸으로 함께 늘렸다 — round-13 에서 `redD` 크기로 데인 바로 그 자리라 얹기 전에 크기부터 봤다**
- **[round-24 · 딸려 풀림] 금속 기울기 100배 역전은 착시였다** — round-17·18·19 세 바퀴를 태우고 미룬 것으로 내렸던 물음이 표류 수정 하나로 풀렸다. 0.01배로 뒤집히던 것이 **0.36~0.66 사이에서 안정**된다. 은하가 판 중앙을 떠난 것을 판 중앙 기준으로 잰 결과였다
- **[round-24 · 다음 바퀴가 할 것] `ashCellsIn`·`ashCellsOut` 을 안 찍었다** — round-18 이 만든 판별식인데 이번 재측정에 안 넣었다. 바깥 구간의 부피가 커서 총량이 큰 것일 수 있어 **칸당 밀도로 봐야 갈린다**

- **[round-23 · 정정] round-22 의 「카메라가 은하를 따라갈 수 없다(차단)」는 절반만 맞았다** — `ClampPan` 이 막는 것은 맞지만 **기능이 없는 게 아니었다.** `keepCenterOfMass`(`App.cpp:323`)가 처음부터 제대로 돌고 있었고, 나는 그것을 찾지 않고 pan 을 손으로 계산해 넣으며 **부호까지 반대로 줬다**(`App.cpp:329` 는 `0.5f - cx`)
- **[round-23 · 재발 방지 확장] round-12 의 「새 키를 넣기 전에 그 키를 이미 다루는지 grep 한다」를 기능 단위로 넓힌다 — 「없다」고 결론짓기 전에 그 일을 하는 이름을 grep 한다.** 이번엔 손으로 만들기 전에 그 일을 하는 것이 이미 있는지 안 찾았다
- [round-23 · 5번 칸] `ClampPan` 을 **따라가기가 켜졌을 때만** 완화 — 근거: 그 한계는 판 밖 테두리를 가리려는 것이고 판 안에 머무는 장면에서는 맞다. 이 판은 은하가 판 모서리까지 흘러가 그 규칙과 부딪힌다. **안 보이는 것보다 테두리가 보이는 것이 낫다.** 여는 폭은 `[-0.5, 0.5]` 까지 — 그 밖은 판이 없어 열어도 검다
- [round-23 · 5번 칸] 설정 창 체크박스로만 켜지던 값을 **제어 채널에 열었다** — 근거: 자동 검증의 손이 안 닿는 값이 있으면 그 값이 관계된 문제는 바퀴가 아무리 돌아도 안 보인다. 실제로 스물두 바퀴 동안 안 보였다
- **[round-23 · 발견] 은하가 판 벽에 눌린 좁은 띠가 된 것을 화면으로 처음 봤다** — round-19 부터 「무게중심이 0.98」이라는 수치로만 쫓던 것의 실제 모습이다. 경계 되튕김(`Sim.cu:588-591`)이 알갱이를 벽에 붙잡는다. **물음이 「좌표가 이상하다」에서 「판이 벽에 눌려 뭉갠다」로 구체적이 됐다**

- **[round-22 · 카운터 정정] 실측하니 29 가 아니라 31 이었다** — round-21 까지의 `[x]` 를 세어 보니 31 done / 12 open. round-15 에서 지적한 「목표 항목만 찍고 곁다리는 round 파일에만 적는」 습관이 또 나왔다. **이번엔 표를 실측값으로 맞춘다**: 31 + 3 = 34, 총계는 새 항목 하나를 넣어 43 → 44
- [round-22 · 5번 칸] 속도 분산과 **공간 두께를 다른 커널로** — 근거: `dispZZ` 는 「위아래로 덜 밀린다」이고 「두께가 생기는가」는 「그래서 판이 얼마나 두꺼운가」다. 속도가 작아도 처음부터 두꺼우면 두껍고 속도가 커도 중력이 되당기면 얇다 — 하나가 다른 하나를 대신 못 한다
- **[round-22 · 발견] 압력은 원반을 부풀리지 않고 얇게 붙든다** — 같은 시각 압력 OFF 쪽이 더 두껍다(20·30·60초 **세 번 다** 같은 방향). 압력이 없으면 알갱이가 서로 스쳐 지나며 위아래로 튀고, 압력이 그것을 되민다
- **[round-22 · 정정] round-10 에서 내가 고친 확인 조건이 틀렸다 — 원래 조건이 맞았다.** 「완전 평면은 압력이 부풀릴 것이 없다」고 추론해 씨앗을 0.001 로 고쳤는데 실측은 씨앗 0 에서도 두께 0.0457 이 생긴다(CIC·격자 이산화가 대칭을 깬다). **재발 방지: 조건을 고치기 전에 그 조건이 정말 만족 불가능한지 한 번 재 본다.** round-10 은 재 보지 않고 추론만으로 고쳤다
- [round-22 · 5번 칸] 「연출·근사」 조건을 **「코드에 없고」 → 「자동 경로에 안 걸리고」로 정정** — 근거: `kOrbitAroundBH`·`blackHoleMassScale` 은 `Sim::addShape`(마우스로 놓기) 안에만 있고 지우면 도구가 망가진다. 항목의 뜻은 제목 그대로 「**끈 상태로** 판이 돈다」다
- **[round-22 · 발견 · 가장 큼] 스크린샷 검증 아홉 개를 막던 것을 찾았다** — 은하가 `cx=0.957` 로 나가 있는데 `ClampPan`(`main.cpp:93`) 의 pan 한계가 zoom 6 에서 0.352 다. 판 테두리를 안 드러내려는 제한이라 **카메라가 은하를 따라갈 수 없다.** 밀도 렌더는 격자 128³ 이라 줌 10 에서 12.8 칸만 들어오는 문제가 하나 더 있다
- **[round-22 · triage] round-19 의 「은하 이사를 미룬 것으로」 판단을 부분 철회** — 그때 「이 문제에 묶인 것은 셋뿐이고 열둘은 독립적」이라 봤는데 **스크린샷으로 확인할 항목이 전부 여기 묶여 있는 것을 못 봤다.** 정체 규칙의 「막고 있는 선행 항목을 먼저 친다」에 따라 **「화면이 은하를 따라간다」를 이번 범위에 새 항목으로** 넣었다. 원인 추적(왜 이사하나)은 미룬 것에 그대로 두고, 화면이 따라가는 것만 친다

- [round-21 · 5번 칸] 밝기 기준을 **평균 → 상위 5% 백분위수**로 — 근거: 평균은 초신성 하나(은하 전체보다 밝다)에 통째로 끌려가 나머지를 검게 만든다. 정렬 없이 구하려고 로그 히스토그램(64칸)을 썼고 비용은 화면 격자 칸 수뿐이다
- **[round-21 · 정체 처리] 「가스구름이 별빛을 받아 빛난다」를 미룬 것으로 내림** — 근거: 두 바퀴 + 접근 교체 1회에도 안 줄었다. **원인은 정규화가 아니라 스케일**이었다(`gas` 10만 단위 vs `starLight`). 두 값을 각각 정규화한 뒤 섞어야 하고 그건 렌더 경로를 다시 잡는 일이다. **코드는 남겨 뒀다** — `report-deferred.md`
- [round-21 · 5번 칸] 스펙 항목 둘을 **하나로 합침** — 「초신성이 터져도 배경이 보인다」의 확인 조건이 「백분위수 기준이 듣는지」였고, 그 백분위수를 이번에 넣었다. 같은 것을 가리키는 두 항목이었다

- [round-20 · 5번 칸] 설계의 **별도 3D 버퍼·FFT 를 안 씀** — 근거: 화면 격자에서 2D 로 흐리면 시선 방향으로 어차피 겹쳐 보여 결과가 거의 같다. 가로·세로로 나눠 43만 회(2D 한 번이면 277만)
- **[round-20 · 실수] 선행 항목을 못 보고 뒤엣것을 먼저 만들었다** — 성운이 화면에 안 나오는 이유는 밝기 정규화가 평균 기준이라 더한 만큼 깎이기 때문이다. **설계 3.3(밝기 백분위수)이 이 항목의 선행 조건인데 스펙에 그 관계가 안 적혀 있었다.** 다음 바퀴는 그것부터 친다

- **[round-19 · 발견] 은하가 판 밖으로 나간다** — 70초에 중심이 (0.5,0.5) → **(0.983, 0.846)**. `outFrac` 이 오른 것은 퍼져서가 아니라 **판 중앙을 떠나서**였고, round-17·18 의 금속 기울기 해석이 통째로 이 착시 위에 있었다
- [round-19 · 5번 칸] 무게중심 정지(`kSubtractMeanVel`)를 넣었으나 **원인이 아니었다** — 뺀 평균 속도가 0.00016 인데 필요한 값은 1.02 다(6000배 차이). 남은 가설은 「광속 근처로 튄 소수가 평균을 끈다」. **수정은 표준 관행이라 되돌리지 않았다**(규칙 9.4) — `report-review-me.md`
- **[round-19 · 정체 발동 → 접근 교체] 이 추적을 「미룬 것」으로 내린다** — 근거: 세 바퀴(17·18·19)를 원인 규명에 썼고 매번 전제가 무너져 물음이 계속 옮겨갔다(재 역전 → 판 퍼짐 → 은하 이사 → 광속 이탈). **남은 15개 중 이 문제에 묶인 것은 셋뿐이고 열둘은 독립적이다.** 구현이 없어서 미충족인 항목(반사광·줌 전환·암흑물질·결정론적 쪼개기)으로 간다

- **[round-18 · 발견] 내 원인 후보 셋이 전부 틀렸다 — 셋이 같은 전제 위에 있었다.** 「안쪽에 뭉친다」를 전제로 셋을 세웠는데 실측은 **바깥에 98.4%**였다. 그 전제 하나가 무너지자 셋이 동시에 무너졌다. **재발 방지: 후보를 나열하기 전에 그것들이 공유하는 전제를 먼저 적고, 그 전제부터 잰다**
- [round-18 · 발견] 폭발 킥도 원인이 아니다 — 킥 0 일 때 오히려 바깥 비율이 **더 높다**(97.5% → 99.1%). **별 형성을 통째로 꺼도 99.0%** 다
- **[round-18 · 정정] round-17 의 「나선팔은 규칙들이 만들었다」를 좁혀야 한다** — 별 형성을 끈 상태에서도 `spiralM2 = 0.47` 이 나온다. 별의 한살이가 아니라 **중력·냉각·압력**이 만든 것이다

- [round-17 · 5번 칸] 창발을 **스크린샷 인상이 아니라 수치로** 재기로 함 — 근거: 「팔이 생겼나」를 눈으로만 판정하면 바퀴마다 기준이 달라진다. m=2 푸리에 진폭은 실제 은하 값(0.1~0.3)과 견줄 수 있는 숫자다
- **[round-17 · 발견] 나선팔이 나왔다(`spiralM2` 0.5~0.8) — 팔을 만드는 코드가 하나도 없는 상태에서.** round-09 에서 `spiralWave` 를 끄고 round-10 에서 `diskPoint()` 의 로그 나선을 지운 뒤의 값이다. **다만 실제 은하(0.1~0.3)보다 강해 「팔」이 아니라 「두 덩어리」일 수 있다 — 스크린샷 대조 전까지는 못 가른다**
- **[round-17 · 발견] 금속 기울기가 실제와 반대다** — 안/바깥이 2.72배 → 0.01배로 뒤집힌다. **원칙 2(빠진 현실을 찾는다)를 발동할 첫 자리이고, 후보는 「재가 흩어지지 않는다」다**(`ashGrid` 에 감쇠도 확산도 없다). `report-review-me.md` 5번

- [round-16 · 5번 칸] 색을 위해 **격자를 안 나눴다** — 근거: `L = M^3.5`·`T ∝ M^0.5` 라 밝기와 온도가 같은 축(질량)에서 나온다. 밝기 격자 하나를 컬러맵이 색으로 읽으면 된다
- [round-16 · 5번 칸] **또 같은 뿌리로 한 번 헛돌았다** — `ApplyLook` 에 흑체를 넣었는데 그건 시작 시 한 번만 불린다(round-11 에서 이미 확인한 사실). `colorBy` 바꾸는 자리에서 `cmap` 도 함께 바꿔야 했다. **round-12(`colorBy` 중복)·round-13(`redD`·`buf` 크기)과 같은 뿌리 — 「짝이 되는 것을 같이 안 봤다」**

- **[round-15 · 발견] 내가 스펙 카운터를 제대로 안 굴렸다** — 각 바퀴에서 **그 바퀴가 목표한 항목만** 찍고, 곁다리로 확인된 것은 round 파일에만 적었다. 그래서 미충족이 실제보다 **여섯 개 많게** 보였고 정체 감지가 그 숫자를 셌다. **재발 방지: 바퀴를 닫을 때 부수적으로 확인된 것도 스펙을 훑어 찍는다**
- [round-15 · 5번 칸] 남은 19개 중 **아홉이 스크린샷 검증**이다 — 색·나선팔·성단·줌 전환은 수치로 못 가른다. 결정 D6(눈과 수치 둘 다)인데 지금까지 수치만으로 밀고 왔다

- [round-14 · 5번 칸] `SimTimings` 필드 이름을 **안 바꿨다**(뜻이 달라졌는데도) — 근거: HUD·`status`·MCP 가 이미 그 이름으로 읽는다. round-12·13 에서 「기존 제약을 안 보고 얹은」 버그를 셋 겪은 뒤라 이번엔 먼저 봤다. 뜻은 주석에 못 박았다
- [round-14 · 5번 칸] 구간 동기화를 **스텝 끝에서 한 번만** — 근거: 구간마다 `cudaEventSynchronize` 를 부르면 재는 행위가 재려는 대상을 바꾼다. `evB` 를 이미 기다렸으므로 앞 이벤트는 끝나 있어 추가 비용이 없다

- [round-13 · 5번 칸] 보존량이 전부 0 → **원인은 `redD` 버퍼(2칸)에 3칸을 쓴 범위 밖 `cudaMemset`** — 근거: `maxCellCount` 만 정상값이 나왔고 그것은 `redI` 를 쓴다. **되는 것과 안 되는 것의 차이를 보자마자 찾았다.** 그 전에 커널을 두 번 고쳤는데 커널은 처음부터 멀쩡했다
- [round-13 · 5번 칸] status 버퍼 2100 → 4096 — 근거: 이 판에서 항목을 열넷 더했고 `snprintf` 가 넘치면 조용히 잘라낸다. 밖에서는 「없는 필드」와 「값이 0」이 구분되지 않는다
- **[재발 패턴] round-12·13 의 버그 셋이 같은 뿌리다 — 「기존 제약을 안 보고 얹었다」.** `colorBy` 중복 처리, `redD` 크기, `buf` 크기. **재발 방지: 기존 배열·버퍼에 얹기 전에 그 크기가 정해진 자리를 먼저 연다**

- [round-12 · 5번 칸] round-11 이 막힌 원인 = **내가 만든 중복** — `colorBy` 처리가 `ControlBridge.cpp` 342(내가 넣음)와 390(원래 있음) 두 곳에 있었고 뒤엣것이 덮어썼다. 기존 처리는 `light` 를 몰라 전부 `Density` 로 떨어뜨린다. **재발 방지: 새 키 처리를 넣기 전에 같은 파일에서 그 키를 이미 다루는지 먼저 grep 한다** — round-11 에서는 `ApplyLook` 을 의심했는데 그건 시작 시 한 번만 불려 애초에 후보가 아니었다

- [round-11 · 5번 칸] 설계 3.2 의 `lightSrc`/`lightOut` **별도 3D 버퍼를 안 만들었다** — 근거: 그 버퍼는 **반사광(별빛 확산)** 에 필요하고, 스스로 빛나는 것만 그리는 이 단계에는 필요 없다. 분산·속력이 쓰는 「알갱이 → 화면 격자」 길에 얹어 비용이 `kScatterDispersion` 과 같다
- [round-11 · 5번 칸] **`colorBy=light` 가 화면에 반영 안 되는 것을 못 풀고 커밋** — 근거: `ApplyLook` 이 덮어쓰는 줄 알았으나 그것은 시작 시 한 번만 불린다(`main.cpp:630`). 미검증을 충족으로 적지 않고 커밋 제목에도 명시했다. 다음 바퀴에서 `set` 경로를 좁힌다

- [round-10 · 5번 칸] **블랙홀 전환을 기본으로 끔**(`starCollapseToBH = false`) — 근거: 켜면 100초에 후보 366개가 나오고 광속 폭주 감시가 판을 멈춰 **다른 검증을 할 수 없다**. `report-review-me.md` 1번의 사람 판단 대기 항목이었으나 진행이 막혀 triage 로 편입해 내가 정했다. 설정으로 되돌릴 수 있게 남겼다 — 사용자 요청(「별이 붕괴돼 블랙홀이 되는 것도 보고싶어」)과 부딪히는 결정이다
- [round-10 · 5번 칸] `ashCoolK` 0.5 → 0.05 — 근거: 되먹임에 제동이 없어 100초에 점유 셀이 34,443 → **147** 로 붕괴했다. 0.05 에서 같은 시각 16,750(114배)
- [round-10 · 5번 칸] 스펙의 「`diskThickness` 를 0 으로 두고」를 **씨앗 0.001 로 정정** — 근거: 완전 평면은 z 속도가 전부 0 이라 σ²zz 도 0 이고 압력이 부풀릴 것이 없다. 대칭이 완벽해 영원히 평면이다. **내가 쓴 확인 조건이 틀린 세 번째 사례**

- [round-09 · 5번 칸] `orbitDispersion` 0 + 압력 ON 을 **한 몸으로** 바꿈 — 근거: 압력을 끈 채 난수만 빼면 원반을 붙잡는 것이 없다. 3조 대조에서 압력 쪽이 점유셀·속도·밀도 세 지표 모두 우세(`round-09-spec-compare.md`)
- [round-09 · 5번 칸] `blackHoleMassScale` 을 설계(1.0)대로 **안 올렸다** — 근거: 마우스 블랙홀만 쓰는 값인데 1 로 하면 50배가 되고, round-08 에서 블랙홀이 이미 판의 96%를 삼킨다. 그 문제가 `report-review-me.md` 1번으로 사람 판단 대기 중이라 50배로 키우는 변경을 먼저 넣지 않았다

- [round-08 · 5번 칸] 커널이 `addBlackHole` 을 못 부르는 문제 → **후보 버퍼로 커널·호스트를 잇는다** — 근거: `addBlackHole` 은 호스트 함수이고 `bhs[]`·`bhCount` 를 만진다. 커널은 자리·질량만 남기고 호스트가 스텝 끝에서 읽어 만든다
- [round-08 · 5번 칸] 자리가 차면 **만들지 않는다**(밀어내지 않는다) — 근거: 설계 2.4. 저절로 생기는 쪽이 남의 자리를 빼앗으면 그 블랙홀이 삼킨 질량이 사라져 보존이 깨진다. 실측에서 `bhMass` 가 증가만 한다
- [round-08 · 5번 칸] 블랙홀 문턱을 `starBHRatio` 로 **수명 눈금과 분리** — 근거: `starSunMass=3` 하나로 둘을 정하면 별 비율을 맞추는 순간 블랙홀이 흔해진다
- [round-08 · 5번 칸] **블랙홀이 판의 96%를 삼키는 것을 고치지 않고 올렸다** — 근거: 삼킴 반경의 격자 한 칸 바닥은 재부팅 여섯 번을 막는 안전장치이고 설계 5장이 「그대로 둔다」로 못 박았다. 안전과 그림이 부딪히는 자리라 사람이 정해야 한다 — `report-review-me.md` 1번

- [round-07 · 5번 칸] **설계 이탈** — `design.md` 1장이 `ash` 를 알갱이 배열(`+ashTmp`)로 잡았는데 **격자로** 구현 — 근거: 폭발은 자기 자신이 아니라 그 자리 주변을 오염시키고, 다음에 그 자리를 지나는 알갱이가 영향을 받아야 사슬이 돈다. 알갱이에 붙이면 터진 것만 재를 갖고 다녀 안 퍼진다. 정렬용 `ashTmp` 도 불필요해짐. `round-07-spec-compare.md`
- [round-07 · 5번 칸] 재 → 냉각률을 **로그로** 연결(`1 + k·ln(1+ash)`) — 근거: 재는 감쇠가 없어 150초에 3,659억까지 쌓인다. 선형이면 그 칸이 절대영도처럼 굳는다. 로그면 배수가 14 배에 머물고 `k > 0.5` 절단이 한 겹 더 있다

## 판단 기록 (round-06)

- [round-06 · 5번 칸] 스펙의 「별 비율 5% 안팎」을 **「평형이 존재하는가」로 정정** — 근거: 5% 에 물리적 근거가 없다(내가 설계 때 넣은 임의값). 실제 은하는 별이 질량의 대부분이고 가스가 10% 남짓이며, 이 판은 이미 뭉친 은하에서 시작한다. `round-06-spec-compare.md`
- [round-06 · 5번 칸] `starSunMass` 100 → 3 — 근거: `ratio < 1` 이면 `ratio^-2.5 > 1` 이라 수명이 태양보다 **길어진다.** 기준이 100 이면 지금 판의 별이 거의 전부 그 구간이라 안 죽는다. 실측 스캔에서 3 만 평형(+1.3%p)
- [round-06 · 5번 칸] **내가 쓴 확인 조건이 틀린 것이 두 번째다**(round-03 「광속 10%」, round-06 「5%」) — 재발 방지: 스펙에 숫자를 적을 때 **그 숫자의 출처**를 함께 적는다. 출처가 「내 어림」이면 그렇게 적는다

**총계 정정**: 41 이 아니라 **44** 다(1차 집계 오류).

다음 증분: **별 문턱(Jeans 조건)**. 설계 순서(압력 → 별 문턱 → 질량 → 폭발 → 재)의
다음 칸이고, 압력이 들어갔으므로 `M_J = k_J·σ³/√ρ` 를 쓸 재료가 다 있다
(`dispX/Y/Z` 가 σ², `dispCnt` 가 ρ). 할 일 넷:
①`kStarForm` 커널 ②`pos.w` 에 별 질량 ③`starCount()` 구현(지금 `return 0`)
④별 비율이 5% 안팎인지 재서 `k_J` 조정.

## 판단 기록 (round-03)

- [round-03 · 5번 칸] 압력 켤 때 분산 갱신 주기 8 → 2 — 근거: 위상 어긋난 힘이 일을 한다는 가설을 세우고 성공 기준(증가율 2.22 미만)을 **먼저 적은 뒤** 검증. 결과 1.65 + `maxSpeed` 가 대조군과 같아짐(`round-03-spec-compare.md`)
- [round-03 · 5번 칸] 비용 +41%(4.01 → 5.67 ms)를 **감수** — 근거: `frameMs` 가 16.67 로 안 변한다(렌더 병목). 수치 가열은 누적되지만 스텝 1.7 ms 는 누적되지 않는다
- [round-03 · 5번 칸] 스펙 확인 조건 「최고속도가 광속의 10% 아래」를 **「대조군과 같은 수준」으로 정정** — 근거: 압력 넣기 전 판이 이미 0.88~0.97 이라 처음부터 만족 불가능한 기준을 내가 적어 두었다. 뜻(압력이 가속하지 않는다)은 유지

## 판단 기록 (이어서)

- [round-01 · 5번 칸] 하드코딩 상한 `cap > 10000000` → 3000만 — 근거: 결정 D8 이 "메모리 한계와 프레임 예산 둘뿐"이라 못 박았는데 이 줄이 셋째 제한이었다. 실측에서 1600만 요청이 1000만으로 잘려 드러남. 상세는 `round-01-spec-compare.md` 「이탈」
- [round-01 · 5번 칸] 자동 테스트 대신 **실측을 합격 게이트로** — 근거: CLAUDE.md 가 `sim_tests.exe` 를 "3D 전환 이후 2D 기준이라 실패한다. 고치기 전에는 돌리지 않는다"로 명시. implement 스킬의 "사람이 승인한 스펙이 다른 합격 게이트를 명시" 예외에 해당
- [round-01 · 5번 칸] 시간 환율을 `dt` 가 아니라 **새 상수**로 — 근거: `dt` 는 CFL 안정성이 쥔 값이라(`Sim::step` 주석) 표시 편의로 움직이면 안정성 근거가 사라진다. 지금 dt 에서 역산해 `kYearsPerSimUnit = 1e7` 을 얻었고 실측이 정확히 일치
- [round-02 · 5번 칸] **설계 이탈 ①** — `design.md` 6·7장은 분산 쌓기를 `kDispScatter` 별도 커널 + **CIC 8칸**으로 잡았는데, 구현은 `kCool` 안에서 **NGP 1칸**으로 했다 — 근거: CIC 8칸 × 3성분 × 400만 = **9,600만 atomicAdd** 로 `kScatter`(3,200만)의 3배가 된다. `kCool` 이 이미 이웃 27칸을 훑은 값이라 그 자체로 국소 평균이고, 한 칸에 쌓아도 정보가 뭉개지지 않는다. 실측 비용 +4.7% 로 확인됨. **커널을 안 나눈 것도 같은 이유** — 이웃 훑기가 비용의 거의 전부라 나누면 가장 비싼 부분을 두 번 한다
- [round-02 · 5번 칸] **설계 이탈 ②** — 분산 격자를 설계의 3개가 아니라 **4개**로(`dispCnt` 추가, 25MB → 33MB) — 근거: 압력 `a = -∇(ρσ²)/ρ` 에 ρ 가 필요한데 밀도 격자(`rho`)는 패딩된 S³ 이고 CIC 가중치라 칸별 알갱이 수가 아니다. `dispCnt` 를 따로 세면 `dispX` 합이 그대로 `ρσ²` 가 되어 나눗셈도 사라진다(`kPressure` 주석)
- [이어하기] 정독 범위: **전부 정독(사용자 선택)** — 다만 `log.md`·`handoff.md`·`requirements.md` 는 이 세션 앞 턴에서 전문을 읽었고 그 뒤 수정된 적이 없어 다시 열지 않았다. `spec-trace.md`·`work.md`·`round-01/02`·`implement-note.md` 는 이 세션에서 직접 쓴 것이라 내용이 맥락에 있다. **이번 턴에 다시 연 것은 `design.md`·`spec.md` 둘** — 가장 많이 고쳐진 문서이고, 실제로 그 정독에서 위 이탈 2건을 찾았다
