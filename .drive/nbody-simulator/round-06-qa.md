# round-06 QA (8번 칸)

대상: `build/Release/nbody.exe` (2026-08-13 19:53 빌드, 소스 19:52 · 커밋 `c6c88f1`)
수단: `mcp/server.js` 11도구로 앱을 띄우고 조작·촬영 (창 입력이 필요한 항목은 별도 표시)
스크린샷: `round-06-qa/` **38장** — 리포트에는 경로만 적는다
축: ①변경분 축(커밋 `473c6ee`~`c6c88f1` 전체) ②화면 완성도 축(보드 9섹션·툴바·HUD)

**QA RESULT: FAIL** — 1차 33 PASS / 10 FAIL → 재측정 후 **37 PASS / 3 FAIL / 4 확인불가**

---

## 1차에서 내가 틀린 판정 (재측정으로 되살림) — 4건

`statusBody`(ControlBridge.cpp:123-137)의 실제 필드명과 대조하지 않고 이름을 지어냈다.

| 항목 | 내가 본 이름 | 실제 이름 | 재측정 결과 |
|---|---|---|---|
| A3 한 스텝 | `steps` | `simTime` (steps 필드는 없다) | **PASS** — simTime 0.00282 → 0.00294 |
| A3b 리셋 | `steps` | `simTime` | **PASS** — 0.02643 → 0 |
| A6 단계별 ms | `msScatter`·`msFrame` | `scatterMs`·`frameMs` | **PASS** — stepMs=1.70 scatter=0.51 poisson=0.60 gather=0.43 frame=3.005 |
| J1 질량 보존 | `gridMass` | `totalMass` | **PASS** — 중력2.0 800스텝 2000000 → 2000000, **상대변화 0.00e+0** |

**이 패턴은 세 번째다** (round-04 중력우물 · round-05 냉각·팽창 · 이번 필드명).
앞의 둘은 「국소 최댓값을 전역 지표로」였고 이번은 「검증 대상의 실제 인터페이스를 안 읽고 지표를 지어냄」이다.
→ `report-review-me.md` 에 **「판정 지표는 대상 코드를 열어 확인한 뒤에 쓴다」**로 올린다.

---

## 확정 결함 — 3건

### QA-1 시간 배속이 통째로 무효다 (A4)

```
200스텝 simTime  x0.25 = 0.02349 (dtUsed=0.000101)
                 x3.0  = 0.02329 (dtUsed=0.000101)
```

`dtUsed` 가 **소수점 여섯째 자리까지 같다.** `Sim::step` 은 `dt = 0.0016f * cfg.timeScale` 로 시작하지만
(x0.25 → 0.0004, x3.0 → 0.0048) CFL 클램프가 그 위에서 0.000101 로 잘라 **timeScale 이 전부 사라진다.**
`applyConfig()` 를 태워도 같다(P1-repro: set 단독 0.000102 / preset 경유 0.000102).
즉 이건 리뷰 P1 #1 의 미전달 문제가 **아니라 별개의 결함**이다 — 값은 코어에 갔는데 CFL 이 덮어쓴다.

스펙 「시간 배속을 조절해 시뮬레이션이 빨라지고 느려지는 것을 본다」가 **실제로는 동작하지 않는다.**

### QA-2 표시 설정을 상태로 되읽을 수 없다 (F-status)

`nbody_set` 은 받지만 `statusBody` 가 안 내보내는 필드: **colormap · brightness · displayGamma · hud · zoom · panX · panY**
설정은 화면에 반영되지만(스크린샷 21~28 로 확인) MCP 로는 확인할 방법이 없어
F3(컬러맵)·F4(밝기/대비)·F5(HUD)·G1(줌팬) 4항목이 **자동 검증 불가**로 남는다.

### QA-3 같은 스텝 수로는 중력 세기 차이가 안 보인다 (C1 — 조건부)

```
같은 물리 시간(t=0.02) : g=0.2 최대밀도 44.98  /  g=1.8 최대밀도 453.76   (10배)  → 물리는 정상
같은 스텝 수(400)      : g=0.2 밀도 453.76     /  g=1.8 밀도 453.76        (동일)
```

CFL 이 강한 중력에서 dt 를 6.6e-5 까지 줄여(약한 중력은 2.0e-4) 같은 스텝이 다른 물리 시간이 된다.
**물리는 맞지만 사용자가 슬라이더를 올렸을 때 "화면이 느려질 뿐 더 세게 뭉치지 않는" 것처럼 보인다.**
QA-1 과 같은 뿌리(CFL 이 시간 제어를 독점)라 함께 다룬다.

---

## 확인 불가 — 4건 (창 입력이 필요)

MCP 는 앱 내부 상태를 조작하므로 ImGui 위젯 클릭 자체는 태우지 못한다.

- 설정 보드 **접기/펼치기** 버튼
- 보드의 **「스냅샷 저장」·「최대치로 맞추기」** 버튼 클릭 (파일 산출은 `nbody_screenshot` 으로 확인 — I1 PASS)
- **창 크기 조절**(`WM_SIZE`) 후 화면 유지
- 마우스 **드래그**로 형태 추가·뿌리기 (좌표 API 로는 PASS — G2~G6)

→ `report-review-me.md` 로 넘긴다.

---

## 화면 완성도 축

`round-06-qa/02-clumped.png` 로 판정.

| 요소 | 결과 |
|---|---|
| HUD (좌상단) | FPS·ms/frame·파티클 수·격자·경계·시각 t·「일시정지」 배지 — **시안대로** |
| 설정 보드 (우측) | 「◈ 접기」 + `X1 · CUDA` 표식, 섹션 헤더 **시뮬레이션·중력·가스·우주·표시·마우스 도구** 가 자리대로 |
| 하단 툴바 | **카메라 / 뿌리기 / 중력우물 / 형태추가 / 지우개** 5개 — 시안대로, 선택 항목 강조됨 |
| 렌더 | 200만 파티클이 원반 구조를 만들고 필라멘트가 보인다 |

**관찰**: 1600×900 에서 보드가 세로로 넘쳐 **「프리셋·녹화·성능」 3섹션이 스크롤 아래**에 있다.
9섹션이 다 존재하고 스크롤로 닿지만, 성능 막대를 보려면 스크롤해야 한다. → `report-review-me.md`

---

## 항목별 결과 (재측정 반영)

**PASS 37** — A1a·A1b·A2·A3·A3b·A6 / B1·B2·B3·B4·B5·B5b / C1(조건부)·C2·C3·C4 /
D1·D2·D3·D4·D5 / E1·E2 / F1·F2 / G2·G3·G4·G5·G6 / H1·H2·H3·H4·H5 / I1·I2 / J1

주요 실측:
- **B3 1000만 파티클 fps=148.55** (1024², 목표 60 대비 2.5배)
- B5 5억 요청 → **88,592,435 개로 클램프**
- D1 압력 off 최대밀도 2585.8 → on 89.08
- D3 평균온도 냉각off 0.462887 → on 0.05539
- D4 별 **1,994,530 / 2,000,000**
- G3 형태 3종 400000 → 700000 → 1000000 **요청 개수와 정확히 일치**
- G6 지우개 1000000 → 600000 → 재추가 800000
- I2 녹화 **48프레임 / 48파일 / 첫 파일 5,626 KB**
- J1 질량 상대변화 **0.00e+0**

**FAIL 3** — A4(QA-1) · F-status(QA-2) · C1 의 스텝기준 관측(QA-3, 물리는 PASS)
**확인 불가 4** — 보드 접기 · 보드 버튼 클릭 · 창 크기 조절 · 마우스 드래그

---

## 줄인 것

없음 — 스펙 「이번 범위」 44항목을 전부 행동 단위로 재현했다.
창 입력이 필요한 4건은 수단이 없어 「확인 불가」로 분리했고 건너뛴 것이 아니다.

---

QA RESULT: FAIL
