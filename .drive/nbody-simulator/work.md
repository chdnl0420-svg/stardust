# 작업 기록 — nbody-simulator

DRIVE_STATUS: in_progress
resume_cron: 11f0e0cb (17,47)
resume_idle_count: 0

## VCS · 베이스라인

| 항목 | 값 |
|---|---|
| 모드 | **Git** (P4 아님) |
| 저장소 | `D:\Project\Nbody` — 이 판에서 새로 `git init` 함 |
| 작업 브랜치 | `feature/nbody-simulator` |
| 베이스라인 커밋 | `35bf7cd` "chore: 설계 산출물과 프로토타입 베이스라인" (추적 25파일) |
| 진입 시 작업 트리 | CLEAN |

`git init` 은 이 판에서 스스로 정했다 — 아래 판단 기록 참조.
푸시·PR 은 하지 않는다(`/CAP:submit` 의 몫). 로컬 커밋까지만 한다.

## permissions_preflight (0번 칸)

`~/.claude/settings.json` 의 `permissions.allow` 대조 결과 **4종 전부 이미 존재**해 추가한 규칙 없음:
`Edit` · `Write` · `mcp__nx3-unity-mcp` · `mcp__Claude_Browser`.
거절된 규칙 없음. `defaultMode: bypassPermissions`.

## 칸 실행 위치

| 칸 | 위치 | 사유 |
|---|---|---|
| 4 spec-trace | **메인 실행** | 이 세션은 사용자 요청 없이 서브에이전트를 띄우지 않는 설정. drive 예외 조항("서브를 쓸 수 없는 세션이면 메인에서 돌고 「메인 실행」으로 적는다") 적용 |
| 6 spec-compare | 메인 실행 예정 | 위와 같음 |
| 7 external-review | 메인 실행 예정 | 위와 같음 |
| 8 app-qa | 메인 실행 | 원래 메인이 원칙 |

## 판단 기록

- [5번 칸 진입 · VCS] git 도 P4 도 아닌 폴더였다 → **`git init` + `feature/nbody-simulator` 브랜치 생성**을 스스로 정함 — 근거: 구현 루프가 여러 바퀴 돌며 되돌릴 일이 생기고, CLAUDE.md 9번(가설 검증 시 파일 원상 복구)이 VCS 없이는 `.bak` 물리 복사에 의존해야 한다. `p4` 실행파일은 있으나 이 폴더는 P4 워크스페이스가 아님(`git rev-parse` NOT_A_GIT_REPO, 트리에 P4 흔적 없음)
- [5번 칸 진입 · .gitignore] 빌드 산출물·`proto/out*`·`*.raw`·`extern/` 을 무시 대상으로 정함 — 근거: 프로토타입 출력이 raw 16개 × 4MB 로 크고 재생성 가능. `.drive/` 는 **커밋한다**(이 프로젝트에서는 작업 기록이 산출물)
- [4번 칸] spec-trace 1회차 FAIL(누락 4건) → 출처를 직접 열어 확인하고 전부 "이번 범위"로 되돌림, 2회차 PASS — 근거: `spec-trace.md` 2회차 수선 내역 표
- [4번 칸] 역방향 미추적으로 올라온 힘 오차 기준 0.15 → 폐기하지 않고 **근거를 design.md §8 에 명시**하는 쪽을 택함 — 근거: 512² 실측 0.134(measurements.md 라운드5)에 여유를 얹은 값이며, 기준 자체는 QA 판정선으로 필요하다

## 바퀴 기록

| 바퀴 | 산출물 | 상태 |
|---|---|---|
| — | `spec-trace.md` 1·2회차 | 4번 칸 PASS |
| round-01 | `implement-note.md`, 커밋 `473c6ee`·`9671ef5`, `round-01-spec-compare.md` | 5번 칸 증분 1 완료 → 6번 칸 **FAIL** (met 25 / 미충족 24) → 5번 칸으로 되돌아감 |
| round-02 | 커밋 `f3e35fe` (MCP 서버 + 제어 채널), `mcp/test-client.js` **11 PASS / 0 FAIL** | 증분 2 완료. spec 카운터 충족 26 / 미충족 18 |
| round-03 | 커밋 `766ae9e`, `round-03-spec-compare.md`, 회귀 19 PASS | 안정성·기반 5항목 완료. 미충족 15 |
| round-04 | 커밋 `6b2dfbd`, `round-04-spec-compare.md`, 회귀 24 PASS + MCP 시나리오 7 PASS | 마우스 개입 6항목 완료. 미충족 **9** |
| round-05 | 커밋 `c6c88f1`, `round-05-spec-compare.md`, 회귀 24 PASS + MCP 가스/우주/표시/녹화 8 PASS | **미충족 0 / 충족 44 — 6번 칸 PASS** |
| round-06 | `round-06-review.md`(codex gpt-5.6-sol), `round-06-qa.md` + 스크린샷 38장 | 7번 칸 **GATE: FAIL**(P1 17 / P2 21) · 8번 칸 **QA FAIL**(37 PASS / 3 FAIL / 4 확인불가) → 5번 칸으로 되돌아감 |
| round-07 증분 1 | 커밋 `1a921ce`, `mcp/bridge-test.js`(신규 영구 회귀), 회귀 **25 PASS** + 브릿지 **5 PASS** | 설정 전달 경로·시간 배속·상태 노출 복구. 되돌린 11항목 중 **8항목 회복 → 미충족 3** |
| round-07 증분 2 | 커밋 `d398231`, `round-07-qa/points-cmap-*.png`, 회귀 **27 PASS** + 브릿지 **5 PASS** | `isStar` 정렬·압축(P1 #5·#6), 주기 경계 소프트닝(P2 #19), 점 렌더 컬러맵(P2 #26). **미충족 0** |
| round-07 증분 3 | 커밋 `3d2ec4d`, 회귀 **27 PASS** + 브릿지 **7 PASS** | 남은 P1 전부: CUDA 오류 차단(#7·#8·#9·#12) · VRAM 추정(#10·#11) · 입력 검증(#13) · MCP 주입·동시성·step 완료·경로(#14~#17). P2 도 #27·#29·#30·#31·#36·#38 처리 |

## 다음 칸

**round-07 증분 2 = 5번 칸 계속** — 미충족 3항목이 각각 남은 P1/P2 에 걸려 있다.

| 미충족 항목 | 걸린 결함 |
|---|---|
| 소프트닝을 조절해 근거리 거동이 달라진다 | P2 #19 — 주기 경계 주파수 커널이 `softeningCells` 를 안 쓴다(`Sim.cu:731`) |
| 별 형성을 켜서 조밀·차가운 가스가 별로 바뀐다 | P1 #5 정렬이 `isStar` 를 안 옮김 · #6 지우개 압축이 `isStar` 를 안 압축 · #4 부분 점유 정렬 불변식 |
| 컬러맵을 천체/흑백/열화상으로 바꾼다 | P2 #26 — 점 렌더 커널에 `cmapKind` 를 안 넘긴다(`RenderField.cu:100`) |

**증분 3(다음)** — 스펙 미충족은 0 이지만 **7번 칸 재리뷰가 아직 못 통과한다.** 남은 P1 8건:
CUDA 오류 처리(#7 로그 후 계속 · #8 cuFFT 계획 실패 무시 · #9 CUB sort 반환값 미확인 ·
#12 `devAccum_` 할당 미확인) · VRAM 추정 누락(#10) · `maxN==0` 클램프 생략(#11) ·
외부 설정 범위 검증(#13) · MCP 명령 주입(#14) · 동시 호출 충돌(#15) · step 완료 미확인(#16) ·
스크린샷 경로 무제한(#17). 끝내면 7(재리뷰) → 8(재QA) → 9(regression) → 10(보고).

- [round-07 · 5번 칸] **판정 지표를 이 바퀴에만 다섯 번 고쳤다** — 소프트닝 하나에서 네 번
  (구조형성 프리셋: 안 뭉쳐 9.21 동일 → 나선팔: `kSetOrbit` 이 궤도속도로 상쇄해 6% → 정지 덩어리
  250스텝: 밀도가 격자 해상도에 포화해 43.6 동일 → **정지 덩어리 150스텝의 최대속력 1.81배**),
  컬러맵에서 한 번(전체 화면 평균색을 쟀더니 우측 설정 보드의 청회색이 섞였다 — 세 컬러맵의
  표본 픽셀 수가 25107 로 **똑같이** 나온 것이 단서였다. 보드·툴바 영역을 빼자 gray 가 R=G=B 로 떨어졌다).
  전부 기준을 낮춘 것이 아니라 **지표가 그 질문에 답하지 못하는** 경우였다.
  이 판에서 지표 오류가 누적 6건이라 `report-review-me.md` 에 「측정하려는 양이 지표에 실제로
  드러나는지부터 확인한다」로 올린다.

- [round-07 · 5번 칸] **`needApply` 플래그를 없애고 매 프레임 동기화로 바꿨다** — 근거: 위젯마다 표시를
  손으로 켜는 구조라 17군데가 빠져 있었고, 같은 실수가 다음 위젯에서 또 난다. `Sim::reconfigure` 가
  이미 `needRealloc` 을 스스로 계산하므로(`Sim.cu:873-877`) 매 프레임 넘겨도 재할당은 안 일어난다.
  상세는 `implement-note.md` 「round-07 이탈 항목」.
- [round-07 · 5번 칸] **배속 > 1 을 dt 확대가 아니라 프레임당 스텝 반복으로 냈다** — 근거: CFL 은 넘으면
  적분이 무너지는 안정성 한계라 dt 를 못 늘린다. 대신 계산량이 배수로 늘어난다는 사실을 보드 안내와
  상태(`stepsPerFrame`)에 드러냈다. **벽시계 시간당 진행량으로는 이 효과가 안 보인다** — 프레임이
  그만큼 오래 걸려 초당 프레임 수가 함께 줄기 때문이다(실측 1.5초에 1.0x=0.012930 / 3.0x=0.015830).
- [round-07 · 5번 칸] **브릿지 테스트 지표를 세 번 고쳤다** — 최대밀도→최대속력(CFL 이 밀도 차이를 지운다),
  벽시계 진행량→프레임당 스텝 수, 점유셀→`activeCount`(프리셋마다 격자 크기가 달라 점유셀을 못 견준다).
  셋 다 "지표가 그 질문에 답하지 못하는" 경우였지 기준을 낮춘 것이 아니다.

- [round-06 · 7·8번 칸] **미충족 0 인데도 판을 안 끝냈다** — 근거: 스펙 카운터는 44/44 지만 리뷰가 P1 17건,
  QA 가 실제 미동작 3건을 찾았다. 스펙 항목이 `[x]` 인 것과 그 행동이 실제로 되는 것은 다르다 —
  「시간 배속을 조절해 빨라지고 느려지는 것을 본다」는 `[x]` 였지만 `dtUsed` 가 x0.25·x3.0 에서
  소수점 여섯째 자리까지 같았다(`round-06-qa.md` QA-1). 스펙 카운터를 되돌려 해당 항목을 `[ ]` 로 내린다.

- [round-06 · 8번 칸] **QA 판정 지표를 내가 지어내 4항목을 오판했다** — `statusBody` 를 열지 않고
  `steps`·`msFrame`·`msScatter`·`gridMass` 라는 이름을 추측해 썼고 전부 없는 필드였다.
  실제 이름은 `simTime`·`frameMs`·`scatterMs`·`totalMass`. 재측정하니 4건 다 PASS.
  → 이번 판에서 판정 지표 오류가 **세 번째**다(round-04 중력우물 · round-05 냉각/팽창 · round-06 필드명).
  앞의 둘은 「국소 최댓값을 전역 지표로」, 이번은 「대상 인터페이스를 안 읽고 지표를 지어냄」.
  `report-review-me.md` 에 재발 방지로 올린다.

- [round-06 · 7번 칸] **리뷰 P1 중 3건을 코드로 직접 검증하고 나머지 14건은 미검증으로 넘겼다** —
  근거: 검증한 3건(`Board.cpp:34` needApply 누락 · `ControlBridge.cpp:258` 동일 · `Board.cpp:194` 프리셋 순서)이
  이미 게이트를 막기에 충분하고, 나머지는 5번 칸에서 고치며 확인하는 편이 같은 파일을 두 번 여는 것을 막는다.

- [재개 판단] 재개 job 의 「자기 종료」 3번(미충족 0 → complete 후 CronDelete)에 걸리지만 **종료하지 않았다** —
  근거: 그 조항은 "판은 끝났는데 마커만 안 바뀐 경우"를 구제하려는 것이고, 지금은 체인 7·8·9·10 칸이
  실제로 미실행이다. 여기서 job 을 지우면 리뷰·QA·회귀·보고가 통째로 날아가며, 이는 drive 규칙이
  명시적으로 금지한 「②칸·게이트를 건너뛰거나(리뷰·QA·대조 생략)」에 해당한다.
  판의 끝은 미충족 0 **이후 10번 칸 보고까지 낸 시점**으로 본다.

## 추가 판단 기록 (round-05)

- [round-05 · 5번 칸] **압력을 평균 밀도로 정규화** — 근거: 질량 정규화(round-03)는 중력만 고쳤고 압력은 여전히 N 에 끌려다녔다. `Impl::invMeanRho()`
- [round-05 · 5번 칸] **온도를 압력에 연결**(`P *= 1+T`) — 근거: 연결 전에는 냉각을 켜도 뭉침이 안 바뀌었다(최대밀도 84.22 vs 84.2). 냉각의 목적이 "식어서 더 뭉친다"인데 압력이 온도와 무관하면 그 경로가 없다
- [round-05 · 5번 칸] **냉각·팽창 계수를 dt 규모에 맞춰 키움**(×3000, ×6000) — 근거: CFL 클램프로 dt 가 1e-5 수준이라 시간 비례 항이 0 에 수렴했다. 물리적 비례(dt 비례)는 유지하되 슬라이더를 올렸을 때 사람이 체감할 크기로 맞췄다
- [round-05 · 5번 칸] PNG 인코더를 **앱에 무압축으로 직접 구현** — 근거: design.md O4 가 외부 의존성을 늘리지 않기로 했다. 파일이 커지지만(1600×900 약 5.6 MB) 저장은 사용자가 누를 때만 일어난다. `src/gfx/PngWriter.cpp`
- [round-05 · 6번 칸] **판정 지표를 두 번 교체**(냉각: 최대밀도 → 점유셀 → 평균온도, 팽창: 최대밀도 → 점유셀) — 근거: 국소 최댓값은 전역 상태를 못 대표한다. round-04 중력 우물과 같은 패턴이라 `report-review-me.md` 에 재발 방지로 올린다

## 추가 판단 기록 (round-03 · round-04)

- [round-03 · 5번 칸] **파티클 질량을 1/N 로 정규화** — 근거: 정규화 전에는 파티클을 늘릴수록 은하 총질량이 같이 늘어 중력이 세졌고 최대속력이 4009 까지 갔다. CFL 이 매 프레임 최대 분할로 돌아 1000만·1024² 가 26.7 ms(예산 초과). 정규화 후 최대속력 7.32, 7.40 ms. `Sim.cu` `Impl::potentialScale()`
- [round-03 · 5번 칸] **CFL 서브스텝 상한을 1 로 정함** — 근거: 실시간 앱은 프레임 예산이 먼저다. 쪼개면 계산량이 배수로 늘어 예산을 그대로 넘긴다(상한 4에서 18.0 ms). 대신 시간 간격만 잘라 계산량 1배를 유지하고 화면 속 시간이 물리가 허용하는 만큼 흐르게 했다. 오프라인 정확도가 필요해지면 이 값을 올린다 — `Sim.cu` `const int MAX_SUBSTEPS = 1;`
- [round-03 · 5번 칸] VRAM 예산을 가용량의 **80%** 로 잡음 — 근거: cuFFT 내부 작업버퍼와 단편화를 감안. 실측 7081MB 가용에서 8,573만 파티클
- [round-04 · 5번 칸] **자유 슬롯을 커서가 아니라 불변식으로 해결** — 근거: design.md §9-1 이 "커서" 를 제안했으나, 지우개가 중간을 비우면 커서만으로는 개수를 보장할 수 없다. 살아있는 것을 항상 앞쪽에 모으고 지울 때마다 정리(CUB DeviceSelect + compaction)를 돌리는 쪽이 불변식이 단순하다. 회귀테스트 [10] 로 120000×3 정확 확인
- [round-04 · 6번 칸] **중력 우물 판정 지표를 최대밀도 → 질량중심 거리로 교체** — 근거: 최대밀도는 국소 최고점이라 우물이 물질을 넓게 모으면 오히려 줄었다(4876 → 3016). 질량중심-우물 거리로 바꾸자 0.3777 → 0.3735 로 정상 판정. MCP 상태에 `centroidX/Y` 추가

## round-02 결과 요약

- 커밋 `f3e35fe` — 파일 기반 제어 채널(`src/app/ControlBridge.cpp`) + MCP 서버(`mcp/server.js`, 도구 9개)
- `mcp/test-client.js` **11 PASS / 0 FAIL** — 실행·상태조회·설정변경·프리셋전환·스텝제어·물리반응·화면캡처·종료
- MCP 로 캡처한 실제 화면 `build/mcp-shot.png` (1600×900) — 설정 보드 9섹션·HUD·툴바가 시안대로 확인됨
- spec 「QA 자동화」 4항목 전부 `[x]`

## 추가 판단 기록 (round-02)

- [round-02 · 5번 칸] 제어 채널을 **소켓이 아니라 파일**로 정함 — 근거: 방화벽·포트 충돌·권한 문제가 붙지 않고, 붙었다 떨어져도 상태가 남는다. 명령 형식도 JSON 대신 `key=value` 줄로 정해 C++ 쪽에 JSON 파서를 들이지 않았다
- [round-02 · 5번 칸] PNG 인코딩을 **앱이 아니라 MCP 서버에서** 하기로 정함 — 근거: 앱에 PNG 인코더를 넣으면 의존성이 늘고, Node 는 zlib 이 내장이라 공짜다. 앱은 RGBA raw 만 쓴다
- [round-02 · 5번 칸] **상태 응답 버그를 실측으로 잡음** — `snprintf` 인자 안에서 측정 세 개를 부르고 있었는데 C++ 는 인자 평가 순서를 정해두지 않아, 격자를 다시 채우는 `measureTotalGridMass` 가 마지막에 실행돼 최대밀도·점유셀이 이전 프레임 값을 읽었다. 증상: 구조형성 프리셋 점유셀 0, 스텝 전후 최대밀도 동일. 명시적 순서로 변수에 담아 해결
- [round-02 · 5번 칸] MCP 테스트 시나리오 오류도 실측으로 잡음 — 프리셋 리셋 *뒤에* 중력을 바꾸면 리셋 시점 중력으로 넣은 궤도 속도만 남아 파티클이 경계로 날아간다(최대밀도 41만). set → preset 순서로 고쳤고, 고친 뒤 중력 0 → 10.93 / 0.8 → 110437 로 정상 반응
- [round-02] **CFL 클램프를 다음 증분 최우선으로 올림** — 근거: 캡처 화면에서 파티클이 경계에 쌓인 것이 눈으로 확인됐다(`build/mcp-shot.png` 가장자리 붉은 점선). 물리가 무너진 상태로는 나머지 항목의 화면 검증이 의미가 없다

## round-01 결과 요약

- 커밋 `473c6ee` 시뮬레이션 코어 + 회귀 테스트 **15 PASS / 0 FAIL**
- 커밋 `9671ef5` Win32 + OpenGL + ImGui 앱, 설정 보드 9섹션, HUD, 하단 툴바
- 앱 실행 확인: 창이 뜨고 5초 후에도 생존(pid 48012)
- 6번 칸 대조: **met 25 / 미충족 24** (MCP 4항목 편입 후 이번 범위 49개)

## 추가 판단 기록 (round-01)

- [round-01 · 5번 칸] UI 갈래에서 `/CAP:frontend-design` 을 **건너뜀** — 근거: 그 칸의 일(팔레트·타이포·레이아웃 결정)을 3번 칸에서 이미 했고 시안이 승인까지 났다(`design-mockup.html`). 다시 부르면 확정된 시안을 재설계하게 된다
- [round-01 · 5번 칸] 테스트 파일을 `.cu` → `.cpp` 로 옮김 — 근거: nvcc 전처리가 UTF-8 한글 문자열 리터럴에서 깨진다(x5.cu 에서도 같은 증상). `Sim.h` 가 Pimpl 이라 CUDA 헤더 없이 컴파일된다
- [round-01 · 5번 칸] `tests/sim_tests.cpp` 를 **영구 회귀 테스트로 남김** — 근거: design.md §8 이 물리 정확도·보존량·토글 반응을 상시 검증 항목으로 정했고 spec.md 에 해당 항목이 있다. implement 2단계 9번의 "영구 회귀 테스트 예외"에 해당
- [round-01 · 6번 칸] 사용자가 도중 요청한 **앱 제어 MCP 를 "이번 범위"에 편입** — 근거: GUI 앱은 현재 스크린샷 외에 자동 검증 수단이 없어 8번 칸 QA 정확도가 여기 묶인다. drive 「새 요구 triage」 규칙상 진행이 막히는 쪽이라 편입하고 사용자에게 되묻지 않았다. 편입 사실은 `report-deferred.md` 에도 적는다
- [round-01 · 6번 칸] 힘 오차 판정선 0.15 를 유지 — 근거: 실측 0.0729(256²)·0.1200(512²)로 판정선 안에 들었다
