# 스펙 추적 리포트 — nbody-simulator

## 1회차 (설계 승인 직후)

### 읽은 파일

| 파일 | 상태 |
|---|---|
| `log.md` | 읽음 (113줄, 덩어리 2개 — 원장 전체) |
| `handoff.md` | 읽음 (굳어진 것 11 / 열린 것 10 / 버린 것 9) |
| `requirements.md` | 읽음 (결정사항 A~G 25항 + 열린 질문 8) |
| `design.md` | 읽음 (열린 질문 8 해소 + §9 프로토타입 발견 4) |
| `design-mockup.html` | 읽음 (설정 보드 9섹션 + 하단 툴바 + HUD) |
| `spec.md` | 대조 대상 (이번 범위 38 / 미룬 것 6 / 버린 것 10) |

**센 결정: 47개** (행동 단위, 중복 표현 병합)

---

### 누락 — 세 구획 어디에도 없음 (4건)

| # | 누락 항목 | 출처 | 들어갈 구획 추정 |
|---|---|---|---|
| 1 | **시간 배속을 조절해 시뮬레이션 속도를 바꾼다** | `design-mockup.html` 시뮬레이션 섹션의 "시간 배속" 슬라이더 | 이번 범위 |
| 2 | **정렬 주기를 조절해 성능과 정확도를 맞바꾼다** | `requirements.md` B6("셀 순서로 주기적 정렬") · `design.md` §4("기본 K=40") · 시안의 "정렬 주기" 슬라이더 | 이번 범위 |
| 3 | **창 크기를 조절해도 정상 동작한다** | `design.md` O6("기본 1600×900, 자유 리사이즈. 렌더 타깃은 창 크기를 따르고 격자 해상도와 독립") | 이번 범위 |
| 4 | **형태 종류를 회전원반·정지덩어리·가스고리 중에서 고른다** | `design.md` O5("형태는 회전원반·정지덩어리·가스고리 3종") · 시안의 "형태" 드롭다운 | 이번 범위 |

2번이 특히 중요하다 — 정렬 주기는 이 설계의 **핵심 성능 근거**(40스텝 재사용이 X1을 B3보다 10배 빠르게 만든 요인)인데
스펙에 조작 항목이 없어 구현·QA 어디서도 확인되지 않는다.

3번은 시안이 격자 해상도와 창 크기를 독립으로 못 박았는데(격자를 바꿔도 창이 안 흔들려야 한다)
스펙에 그 항목이 없어 회귀가 잡히지 않는다.

---

### 구획 충돌 — 없음

같은 항목이 두 구획에 동시에 있는 사례 없음.
`B3 (PM+SPH)` 는 "버린 것"(후보로서 탈락)과 "미룬 것"(B3에 대한 그림 축 측정)에 각각 있으나
**서로 다른 항목**이다 — 하나는 채택 여부, 하나는 미측정 사실의 기록이라 충돌이 아니다.

### 이유 없는 버림 — 없음

"버린 것" 10건 전부 이유가 붙어 있다. 실측 근거가 있는 것(Barnes-Hut·멀티그리드·B3·WebGPU)은
`method-search/scorecard.md` 수치를 함께 인용하고 있어 추적 가능하다.

---

### 역방향 미추적 — 스펙에만 있고 앞 칸에서 근거를 못 찾음 (1건)

| 항목 | 내용 |
|---|---|
| 힘 오차 허용 기준 **"0.15 이하"** | `spec.md` "격자 중력이 직접 O(N²) 대비 허용 오차 안에 있다 — 기준: 소프트닝 3셀에서 0.15 이하". `design.md` §8 은 측정 *방법*만 적고 기준값이 없고, `method-search/measurements.md` 의 실측은 소프트닝을 셀에 비례시킨 조건에서 0.077~0.259 다. 0.15 라는 문턱이 어디서 왔는지 문서에 없다 |

FAIL 사유는 아니다. 다만 이 수치가 QA 판정선이 되므로 근거를 남기거나 실측 분포에 맞춰 다시 정하는 편이 낫다.

---

### 열린 질문 미반영 — 없음

`requirements.md` 의 열린 질문 8개(O1~O8)는 `design.md` §5 에서 전부 해소됐고,
해소된 결정이 스펙에 반영돼 있다. **단 O5·O6 의 세부(형태 3종·창 리사이즈)가 위 누락 3·4번이다.**

---

SPEC-TRACE: FAIL

---

## 2회차 (drive 가 1회차 지적을 수선한 뒤)

### 수선 내역

| 1회차 지적 | 조치 | 반영 위치 |
|---|---|---|
| 누락1 시간 배속 | 이번 범위에 추가 | `spec.md` 기반 섹션 — "시간 배속을 조절해 시뮬레이션이 빨라지고 느려지는 것을 본다" |
| 누락2 정렬 주기 | 이번 범위에 추가 | `spec.md` 규모와 격자 — "정렬 주기를 조절해 성능과 정확도를 맞바꾼다". 확인 방법에 라운드4 실측 재현을 명시 |
| 누락3 창 리사이즈 | 이번 범위에 추가 | `spec.md` 기반 섹션 — "창 크기를 조절해도 화면이 정상으로 유지된다". 격자 해상도와 독립임을 확인 조건에 포함 |
| 누락4 형태 3종 | 이번 범위에 추가 | `spec.md` 마우스 개입 — "형태 종류를 회전원반·정지덩어리·가스고리 중에서 골라 추가한다" |
| 역방향 미추적: 힘 오차 0.15 | 근거를 설계에 명시 | `design.md` §8 — 512² 실측 0.134 에 여유를 얹은 값임을 실측 분포(256² 0.077 / 512² 0.134 / 1024² 0.207 / 2048² 0.259)와 함께 기록 |

**들어갈 구획은 1회차 추정을 그대로 쓰지 않고 출처를 직접 열어 확인했다** — 4건 모두
`design.md`·`design-mockup.html`·`requirements.md` 에서 이미 합의된 항목이라 전부 "이번 범위"가 맞다.
범위 확대가 아니라 전사 누락 수정이다.

### 재대조 결과

| 판정 항목 | 결과 |
|---|---|
| 누락 | **0건** (1회차 4건 전부 반영 확인 — `spec.md` 19·20·27·59행) |
| 구획 충돌 | 0건 |
| 이유 없는 버림 | 0건 (버린 것 10건 전부 이유 있음) |
| 역방향 미추적 | 0건 (0.15 기준의 근거가 `design.md` §8 에 생김) |
| 열린 질문 미반영 | 0건 |

**센 결정 47개 전부가 이번 범위(42) / 미룬 것(6) / 버린 것(10) 중 정확히 하나에 있다.**

SPEC-TRACE: PASS
