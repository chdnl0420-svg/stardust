# round-07 스펙 대조 (9번 칸 · regression check)

대상: `spec.md` 「이번 범위」 **44항목**
코드: 커밋 `7a47f02`(문서) · `3d2ec4d`(오류·자원·입력) · `d398231`(별표식·소프트닝·컬러맵) · `1a921ce`(설정 전달)
판정 질문: **"다 구현했나"가 아니라 "고치다 스펙을 깨먹었나"**

**VERDICT: PASS** — met 44 / 미충족 0 / 회귀 0건

---

## 이번 바퀴가 건드린 것과 그 회귀 위험

`needApply` 제거는 **모든 설정 항목의 경로를 바꿨고**, 주기 경계 소프트닝은 **주기 경계의 물리를 바꿨다.**
둘 다 스펙 전반에 닿으므로 이전 라운드의 검증 스크립트를 **그대로 다시 돌려** 확인했다.

| 검증 수단 | 덮는 스펙 구획 | 결과 |
|---|---|---|
| `tests/sim_tests.cpp` (회귀 13묶음) | 기반·규모·중력·안정성 | **27 PASS / 0 FAIL** |
| `mcp/test-client.js` | QA 자동화 4항목 + 설정·프리셋 | **14 PASS / 0 FAIL** |
| `mcp/tools-demo.js` | 마우스 개입 7항목 | **7 PASS / 0 FAIL** |
| `mcp/gas-demo.js` | 가스·우주·표시·녹화 8항목 | **8 PASS / 0 FAIL** |
| `mcp/bridge-test.js` (이번에 신설) | 설정 전달 경로·배속·MCP 보안 | **7 PASS / 0 FAIL** |
| `round-07-qa.md` 재QA | 손댄 다섯 갈래 + 성능 | **5 PASS / 0 FAIL** |

**합계 68 PASS / 0 FAIL.**

## 깨질 만했는데 안 깨진 것 — 항목별 근거

| 위험 지점 | 왜 위험했나 | 확인 |
|---|---|---|
| **매 프레임 `sim.reconfigure(cfg)`** | 프레임마다 `clampToVram` → `cudaMemGetInfo` 가 돈다. 「1000만 60 FPS」가 깨질 수 있었다 | 재QA Q5 **153.6 FPS**(round-06 148.55 와 차이 없음) |
| **주기 경계 소프트닝 감쇠** | 「구조 형성 프리셋에서 우주 거미줄이 자란다」의 그림이 달라진다 | gas-demo 팽창 항목 PASS(점유셀 비교) · test-client 프리셋 전환 PASS |
| **`kReorder`·`kCompact` 시그니처 변경** | 「형태를 여러 번 추가해도 정확한 개수」·「지우개」가 깨질 수 있었다 | 회귀 [10] **120000×3 정확** · tools-demo **300000 추가 후 1,590,857** |
| **VRAM 추정 증가**(누락 버퍼 반영) | `maxParticlesFor` 가 줄어 「VRAM 클램프」 판정선이 바뀐다 | 회귀 [9] PASS · round-06 QA 의 5억 → 클램프 동작 유지 |
| **`Sim::step` 의 실패 가드** | 정상 경로에서 조기 반환하면 아무것도 안 돈다 | 회귀 27건 전부 PASS(모든 물리 항목이 실제로 진행) |
| **MCP `step` 완료 대기** | 시나리오가 타임아웃할 수 있었다 | 세 시나리오 29항목 전부 PASS(시간만 늘어남) |
| **스크린샷 경로 제한** | 기존 스크립트가 쓰던 `build/shots*` 가 막힐 수 있었다 | 프로젝트 폴더 안이라 통과 — gas-demo 스크린샷 11장 정상 |
| **`statusBody` 필드 추가**(7+2개) | 버퍼 1400 바이트를 넘기면 잘린다 | 1700 으로 늘림. 모든 시나리오가 필드를 정상 파싱 |

## 미충족

**없음.** 44항목 전부 `[x]` 이고, 각 항목의 근거는 `spec.md` 본문에 있다.

round-06 에서 되돌렸던 11항목은 round-07 에서 전부 회복됐다 —
증분 1 에서 8항목(설정 전달·배속), 증분 2 에서 3항목(소프트닝·별 형성·컬러맵).

## 스펙 밖 징후

없음. 리뷰가 지적한 P2 잔여 항목은 스펙 「미룬 것」에 해당하지 않는 **품질 개선**이라
`report-deferred.md` 「리뷰가 지적했으나 이번에 안 고친 것」에 별도로 적었다.

## 줄인 것

없음 — 이전 라운드 검증 스크립트를 하나도 빼지 않고 전부 재실행했다.

---

SPEC-COMPARE: PASS
