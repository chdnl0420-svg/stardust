# 빌드 산출물
build/
out/
*.exe
*.obj
*.lib
*.exp
*.pdb
*.ilk
*.log

# 프로토타입 출력 — 재생성 가능하고 크다
proto/out/
proto/out2/
proto/out3/
*.raw

# vendoring 한 외부 라이브러리 (Dear ImGui 등)
extern/

# 에디터
.vs/
.vscode/
*.user

# 배포 산출물 — 저장소가 아니라 릴리스로 올린다
dist/
*.bak

# 앱이 실행 중 만드는 것 — 스냅샷·녹화 프레임(main.cpp CaptureIfAsked)
captures/

# 도구가 만드는 잠금 파일
.claude/scheduled_tasks.lock
