// 설정을 껐다 켜도 남기기 — Adapter 층.
//
// 사용자가 맞춰 놓은 값이 앱을 다시 켤 때마다 초기값으로 돌아가면, 쓸 때마다 같은 조절을
// 반복하게 된다. 앱을 끝낼 때 한 번 쓰고 켤 때 한 번 읽는다.
//
// 저장 위치는 설치 폴더가 아니라 사용자 폴더다 — 설치본은 자동 업데이트가 실행 파일을
// 갈아 끼우는 자리라, 거기에 두면 판올림 때 설정이 함께 날아갈 수 있다.
#pragma once

#include "app/App.h"

// 저장된 설정을 읽어 app 에 채운다. 파일이 없으면 아무것도 하지 않는다(첫 실행).
void LoadPrefs(App& app);
// 지금 설정을 파일로 남긴다. 앱을 끝낼 때 부른다.
void SavePrefs(const App& app);
