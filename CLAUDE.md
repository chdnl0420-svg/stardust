# Stardust — 이 저장소에서 지켜야 할 것

CUDA 로 수백만 개의 알갱이를 실시간으로 굴리는 앱이다. **이 프로그램의 버그는 앱이 죽는 것으로
끝나지 않고 사용자의 컴퓨터를 통째로 재부팅시킨다.** 아래 규칙은 전부 그렇게 죽은 뒤에 얻은
것이라, 하나하나에 실제 사고 기록이 붙어 있다.

---

## 0. 이 프로젝트에서 시스템이 죽은 여섯 번

| 날짜 | BugCheck | 원인 |
|---|---|---|
| 2026-08-13 | `0xD1` 주소 `0x80` | 매 프레임 `glTexImage2D` — 초당 2.3 GB 를 잡았다 버림 |
| 2026-08-13 | `0xD1` 주소 `0x80` | 같은 원인(앱이 가만히 돌기만 하는 동안 죽음) |
| 2026-08-13 | 재부팅 | 파편 링버퍼의 정수 오버플로 → 배열 밖 쓰기 |
| 2026-08-14 | `0x139` 인자 3 | `kAccrete` 범위 검사 누락 + 미초기화 버퍼 |
| 2026-08-14 | `0x139` 인자 3 | 알갱이 하나가 625 픽셀을 칠함 — 한 프레임에 9억 회 원자연산 |
| 2026-08-14 | `0xD1` 주소 `0x80` | 절반 해상도가 매 프레임 뒤집혀 버퍼를 다시 잡음 |
| 2026-08-16 | `0x139` 인자 3 | **측정 스크립트가 앱을 안 닫아 방치** — 별 질량 상한이 없어 한 칸에 몰린 수가 그대로 질량이 됨(33만) |

**같은 서명이 네 번 나왔다.** 서명이 겹치면 원인도 겹친다 — 새 버그가 아니라 **막았다고
생각한 자리가 다른 경로로 다시 열린 것**이다.

**2026-08-16 이 남긴 것 셋:**

1. **`nvlddmkm` 이벤트가 0 건이어도 죽는다.** 그날 로그에 경고가 하나도 없었다. 4번 항목의
   「Event 153 이 먼저 찍힌다」는 **한 패턴일 뿐이고 전부가 아니다** — 로그 0건을 안전의
   근거로 쓰지 않는다.
2. **워치독은 원리적으로 「한 방에 튀는」 것을 못 막는다.** 스텝 시간은 스텝이 끝나야
   재는데, 그 스텝 도중에 드라이버가 리셋되면 잴 기회가 없다. **막는 것은 감시가 아니라
   값에 건 상한이다.**
3. **앱을 띄우는 측정 스크립트는 `try/finally` 로 종료를 보장하고, 종료를 확인한다.**
   마지막 줄에 quit 을 적는 것으로 부족하다(예외가 나면 건너뛴다). "quit 을 보냈다"가
   아니라 `Get-Process` 가 0 을 돌려주는 것까지가 종료다.

---

## 1. GPU 버퍼는 커질 때만 다시 잡는다 — 줄어들 때는 그대로 둔다

`cudaMalloc`/`cudaFree`, `glTexImage2D` 를 **프레임마다 부를 수 있는 자리에 두지 않는다.**

- 크기가 오르내리는 값(해상도 배율, 품질 단계)으로 버퍼 크기를 정하면, 그 값이 문턱 근처에서
  진동하는 순간 매 프레임 재할당이 된다
- **잡는 것은 최대 크기 기준으로 한 번, 쓰는 것은 그 앞부분만.** 텍스처도 같다 —
  저장소는 큰 크기로 만들고 `glTexSubImage2D` 로 쓴 만큼만 올린 뒤 텍스처 좌표로 잘라 편다
- 상태를 뒤집는 조건에는 **이력(hysteresis)** 을 둔다. 켜지는 선과 꺼지는 선을 벌리고,
  바뀐 뒤 일정 프레임은 그대로 유지한다

판정 질문: **이 할당이 한 시간 동안 몇 번 일어나는가?** 답이 "화면 크기가 바뀔 때만"이
아니면 틀린 자리에 있다.

## 2. 한 스레드가 하는 일에 상한이 없으면 그 커널은 언젠가 시스템을 죽인다

커널을 쓰는 자리에서 **한 프레임에 몇 번 도는지 계산해 주석에 적는다.** 예:

```
// 비용: N 스레드 × 8 atomicAdd = 8N. N=200만이면 1600만 회.
```

- 반지름·개수 같은 값을 **사용자 설정 × 배율**로 정할 때는 그 곱의 최댓값을 계산한다.
  최댓값 × 스레드 수가 억 단위를 넘으면 상한을 걸어야 한다
- 루프 안에서 이웃을 훑을 때는 **훑는 개수 자체에 상한**을 둔다(뭉친 자리에서 폭주한다)
- 정규화 상수를 구하려고 같은 범위를 한 번 더 도는 코드는 특히 위험하다 — 닫힌 식이나
  상수 표로 바꾼다

드라이버 타임아웃(TDR)은 기본 **2초**다. 한 프레임이 그것을 넘으면 드라이버가 강제
재시작되고, 그 과정에서 커널 자료구조가 깨져 `0x139` 가 난다.

## 3. 한계를 아는 쪽이 코어이므로 코어가 자른다

밖에서 들어오는 값(설정, 테스트, 제어 채널)을 **믿지 않는다.**

- 격자 한 변, 알갱이 수, 반지름은 코어가 상한을 강제한다. 요청이 넘치면 자르거나,
  자를 수 없으면 **아예 판을 열지 않고 실패로 표시**한다
- 3D 는 한 변을 하나 올릴 때마다 칸이 여덟 배다. 2D 시절 값(2048, 4096)이 그대로 들어오면
  687억 칸을 잡으려 든다 — 2026-08-14 에 회귀 테스트가 그 값을 요청했다가 시스템 메모리가
  통째로 치솟았다
- 할당 전에 `estimateBytes` 로 재고 가용량과 견준다. 격자만으로 넘으면 알갱이는 하나도
  못 잡는다

## 4. 「돌아가는 것처럼 보인다」는 안전의 근거가 아니다

- 프레임이 60 나온다고 카드가 버티는 것이 아니다. 실측에서 3000만 개가 60 FPS 로 돌았지만
  그 시각에 이미 `nvlddmkm` Event 153 이 찍혀 있었고 53분 뒤 재부팅됐다
- **GPU 관련 작업을 완료로 보고하기 전에 이벤트 로그를 확인한다.**
  ```
  Get-WinEvent -FilterHashtable @{LogName='System'; ProviderName='nvlddmkm'} -MaxEvents 5
  Get-WinEvent -FilterHashtable @{LogName='System'; Id=1001} -MaxEvents 3
  ```
- 무거운 설정으로 시험할 때는 **작은 값부터 단계적으로** 올리고, 매 단계 프레임 시간과
  GPU 온도·사용률을 본다

## 5. 앱을 띄워 시험할 때

- **시험 전에 기존 프로세스를 반드시 정리한다.** 두 개가 뜨면 VRAM 을 두 배로 잡는다 —
  실제로 그 상태에서 시스템이 죽었다
  ```
  Get-Process Stardust -ErrorAction SilentlyContinue | Stop-Process -Force
  ```
- `Get-Process` 결과를 그대로 쓰지 말고 `@(...)[0]` 로 하나만 집는다(여러 개면 배열이 된다)
- 회귀 테스트를 돌리기 전에 그 테스트가 요청하는 값이 지금 코어의 상한 안에 있는지 본다

## 6. 그 밖에

- **PowerShell 정규식 일괄 치환을 소스에 쓰지 않는다.** 한 번은 `ControlBridge.h` 를
  통째로 망가뜨렸고, 한 번은 `.rc` 파일의 한글 주석을 전부 깨뜨렸다. 편집은 편집 도구로 한다
- 링버퍼·커서는 **더할 때마다 범위를 확인**한다. `atomicAdd` 결과를 그대로 인덱스로 쓰면
  정수 오버플로로 배열 밖에 쓴다
- 새 파일을 `cudaMalloc` 한 직후에는 **반드시 초기화**한다. 미초기화 인덱스 배열이
  범위 밖을 가리켜 죽은 적이 있다
- 서밋·푸시·PR·파일 삭제·배포는 사용자 승인 없이 하지 않는다

---

## 빌드와 시험

```
cmake --build build --config Release --target Nbody
```

회귀 테스트는 `build\Release\sim_tests.exe`. **3D 전환 이후 아직 2D 기준이라 실패한다** —
고치기 전에는 돌리지 않는다(위 3번 참조).
